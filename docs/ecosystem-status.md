# Ecosystem Health Status

> This file is generated by the
> [Ecosystem Health Dashboard](../.github/workflows/ecosystem-health.yml)
> workflow. Last updated: _never (run the workflow or recipe to populate)_.

Run `just ecosystem-health` locally, or trigger the GitHub Actions workflow
(`workflow_dispatch` with `update_status_doc = true`), to generate the latest
report. The workflow also publishes the report as a downloadable artifact and
in the run's job summary on every scheduled run.
