# Agent-Instruction Modernization Disposition Ledger

This ledger binds the governance tranche that must be reviewed before
repository-by-repository modernization begins. It records decisions and
boundaries, not implementation, deployment, or live-state evidence.

## Inventory binding

- Repository: `HomericIntelligence/Odysseus`
- Base branch: `main`
- Immutable base: `ccce2ccb4fbd0e12562e96c564d43ca3395e6ce4`
- Delivery branch: `codex/layered-agent-governance`
- Review artifact: supplied by the immutable pull request and commit at publication
- Inventory date: 2026-09-19

At this binding, `.gitmodules` names 15 canonical component repositories. With
Odysseus, that is the same 16-repository set recorded in
`configs/github/fleet-ruleset-policy.json`: AchaeanFleet, Agamemnon, Argus,
Athena, Charybdis, Hephaestus, Hermes, Keystone, Mnemosyne, Myrmidons, Nestor,
Odysseus, Odyssey, Proteus, Scylla, and Telemachy.

The repository rows below bind separate clean current-remote snapshots rather
than treating the older Odysseus gitlinks as current component contents. The
inventory used hidden and tracked-file searches plus repository-specific
loader, manifest, generator, test, workflow, and organization-consumer searches
at each recorded SHA. Those reads authorize no downstream edit. A downstream
PR must revalidate its own exact head before changing or deleting anything.

The in-repository source binding also includes
`configs/schemas/dispatch-envelope.hi-v1.schema.json` and
`workflows/m0-contracts.yaml` at the Odysseus base. Their changes in this
governance overlay include schema-description corrections and semantic authored-
task manifest changes to task descriptions, four subject identifiers, and
matching `blocked_by` references, as bounded by G-10. Task IDs, titles, role and
repository assignments, dependency graph, models, defaults, and task count are
preserved.

Remote checks found that Hephaestus and Argus had advanced after the first
inventory, and successive pre-publication checks found two more Hephaestus
advances. Both repositories were re-inventoried at the newer immutable heads
below. Across its deltas, Hephaestus adds a local Fleet build-artifact
server/library, explicit host-capability checks, first-publication and rebase
recovery, their tests, and revised architecture/contribution guidance. Its
latest delta correctly makes branch-name formatting advisory, removes that
editorial requirement from the documentation policy, and adds behavioral
configuration-policy tests. Its root contract is now 934 lines and its exact
compatibility pointer is unchanged; none of the deltas adds or alters an agent
prompt-composition consumer. Its executable prompt consumers remain internal,
while preserved Mnemosyne entries remain documentation consumers. Argus's
delta adds an opt-in Fleet observation collector and tests; its 517-line
contract, legacy settings, Atlas assets, and external Atlas consumers are
unchanged. GitHub code search remained supplemental to the exact tracked-tree
reads.

| Repository | Immutable current remote SHA | Inventory classification |
|---|---|---|
| Odysseus | `ccce2ccb4fbd0e12562e96c564d43ca3395e6ce4` | Governance base; exact pointer already present; 15 gitlinks plus Fleet web application and coordination surfaces. |
| Athena | `3ba737517e2234729bcc7eb45b6ec0e3531efdf0` | 17 public skill IDs; plugin setting already canonical; noncanonical pointer and large mirrored/procedural instruction surfaces. |
| Mnemosyne | `366015d7b78f522b6bc68f4de67b22229f9e7c4d` | 786 entries, 183 notes, 318 histories; corpus tree `1e46f9fac1e46d0e0b16492a971e219d5d5de4fd`; noncanonical pointer; canonical Athena setting. |
| Hephaestus | `8f4cb45c5d6ce28e6a7028705274e26a83c34a19` | Exact pointer; 934-line contract, legacy settings, internal executable prompt consumers, Athena contract manifest, Fleet build-artifact and host-capability/publication/rebase recovery code, plus the branch-guidance/doc-policy repair and focused tests. |
| Agamemnon | `e45d8dbb6b07756d17a9a0ec375f88f8eb753a95` | Exact pointer; legacy settings; API/store/client/reconciler/scaffolder and prompt artifacts inventoried. |
| Telemachy | `b6af623a041e5cc6c5fe63efaf3e3bec75becfa9` | Exact pointer; legacy settings; local/Docker models, workflow manifests and obsolete artifacts inventoried. |
| Myrmidons | `a0a8224647a98d6ec171aea32782cdde858d586c` | Exact pointer; 455-line contract; 51 agent manifests, four templates, eight fleets, six pools, four schemas, and protected dynamic loaders. |
| AchaeanFleet | `ede530d7645d721bec0fd3d3b875bc6a959b624c` | Exact pointer; 252-line contract; 14 Dockerfiles, six Compose manifests, two pod manifests, Nomad/Dagger/runtime and protected CI surfaces. |
| Nestor | `9b194ca9c8885bdcb1a12f5ae37547fdc72132b8` | Exact pointer; 147-line contract; CMake/API/NATS/docs and hosted-check evidence inventoried. |
| Argus | `a002576a88e8ac8c85d7e475a1fb2d9a7b13382d` | Exact pointer; 517-line contract; Atlas scripts/charter/tests plus external consumers and the new opt-in Fleet observation exporter/tests inventoried. |
| Hermes | `cf3dbffecb3645d605d87384575789d875431aa5` | Exact pointer; 487-line contract; README/OpenAPI, invariant, Markdown/suppression checks and duplicate-number Accepted ADRs inventoried. |
| Charybdis | `2891cd4f9a9618ac639cfe7e64cf36293716816b` | Exact pointer; 269-line contract; fault authorization, cleanup, sanitizer, roadmap and external chaos consumers inventoried. |
| Keystone | `26e507845b9cb4c4f6db39a8eea84bee77b20949` | Exact pointer; 405-line contract plus 143-line `.clinerules`; 34-agent hierarchy, scaffolding, notes and C++ rules inventoried. |
| Proteus | `3e952b6e48c1ccb34a010a60a9ccaac8ab4cc645` | Exact pointer; 310-line contract; four transient artifacts, validation/security/dispatch/docs surfaces inventoried. |
| Odyssey | `a8e1038001fa086a7df30874634b16a4dce7c977` | Exact pointer; 1,119-line contract; 29 roles, planning consumers, prompts/hooks/skills/marketplace/migration and external frozen consumers inventoried. |
| Scylla | `81d8292939dbafded7f1aca0530143a814e06334` | Exact pointer; 623-line contract; eight operational agents, adapters, 40 legacy skills, benchmark history and frozen evidence trees inventoried. |

Reproducible inventory commands, run from each clean detached repository at the
recorded SHA, were equivalent to:

```bash
git status --short
git ls-files
rg --hidden --glob '!.git/**' 'AGENTS\.md|CLAUDE\.md|SKILL\.md|defaultPrompt|programArgs|model|prompt|agent'
rg --hidden --glob '!.git/**' 'loader|package|manifest|generator|scaffold|workflow|test|validate|lint'
git ls-tree -r --full-tree HEAD
git ls-remote --symref origin HEAD
repo_name="$(basename "$(git remote get-url origin)" .git)"
gh api "repos/HomericIntelligence/${repo_name}/commits/main" --jq .sha
gh search code '"AGENT_PROGRAM"' --owner HomericIntelligence
gh search code '"AGENT_HEADLESS_FLAG"' --owner HomericIntelligence
gh search code '"atlas-aggregate"' --owner HomericIntelligence
gh search code '"claude-followup-103"' --owner HomericIntelligence
```

GitHub code-search negatives were treated as supplemental because indexing can
lag; tracked-tree and exact-SHA API reads remained the primary evidence. The
exact queries and path/line results are summarized by the finding rows below.

## Dispositions

| ID | Finding | Chosen action | Owner | Protected boundary | Verification |
|---|---|---|---|---|---|
| G-01 | Two Proposed ADRs used number 009, while current `main` already assigns 021 to the Fleet proposal. | Keep NATS authentication as ADR-009; preserve Fleet ADR-021 byte-for-byte; renumber the Nomad-deferral proposal to ADR-023 and update live references. | Odysseus / this governance change | No Accepted or Superseded ADR body changes. | Unique-number, tracked-reference, status, and immutable-body checks. |
| G-02 | The ecosystem lacks one reviewed authority for concise, provider-neutral instructions and narrow provider supplements. | Add Proposed ADR-022. Dependent repository implementation remains blocked until a separate reviewed status change records formal human acceptance. | Odysseus / this governance change | No model, provider, lane, role, wire, parser, or runtime default changes. | Markdown, link, status, and proposal-only wording checks; human decision still required. |
| G-03 | The current NATS leaf shape uses spoke-local named accounts but lands an authenticated leaf in hub `$G`; v2.10.22 rejects the scalar `token`, current gates miss it, executable pins drift, exact API subjects do not constrain authority-bearing bodies, and migration snapshots need explicit confidentiality and identity boundaries. | Add Proposed ADR-024: one `HOMERIC` application account and local `SYS`; preserve Hermes ownership of agents/tasks/deadletter and Agamemnon ownership of the other four streams; require a complete-body-validating helper, a currently supported exact release after fresh official advisory/upgrade review, one identity per old source account plus a separate target identity, source-local capture and target-local restore, protected exact-version snapshot evidence, and only inventoried NATS-managed JetStream plumbing rather than an operator-authored application bridge. | Odysseus / this governance change | No canonical NATS config, credential, executable pin, protected workflow, or live broker edit. Acceptance and implementation are separate changes. | Proposal/source-anchor review now; real hub-and-leaf parsing, `$G` regression, helper authorization, archive confidentiality/capacity/deletion, per-identity revocation, full-state migration, and operator evidence remain implementation gates. |
| G-04 | Current harnesses mount Claude/GitHub configuration, inject an API key in the multi harness, weaken credential modes in the single harness, give candidate containers network egress and remote Git/GitHub effects, and have no host provider broker. | Add Proposed ADR-025 selecting an atomic migration to a host-only provider broker, owner-only detached archive staging, an isolated raw-byte forge/signing path, Linux/rootless Podman `--network none` with subordinate-only maps and private tmpfs, exact launcher/listener transfer, a conmon-accounted FD3 gate protocol, receiver-relative credential checks, redacted evidence, a broker worker directly bound to controller/supervisor lifetimes with bounded stop/kill cleanup, conservative reconciliation of uncertain provider requests, observable second-pod isolation, and receipt-last cleanup. | Odysseus / this governance change | No broker/runtime implementation, credential movement, default change, protected-workflow edit, deployment, remote effect, or harness retirement. | Exact-base fixtures plus portable tests and real rootless-Podman Linux CI must prove credential absence, sealed host effects, staging/forge hostility, namespace/map/descriptor/gate isolation, direct broker lifelines and FD closure, pre-issue versus uncertain-post-issue provider handling, redaction, cleanup, and the loopback-up second-pod oracle after acceptance. |
| G-05 | The ADR indexes disagreed with ADR bodies: ADR-007 is Accepted, NATS ADR-009 and ADR-010 were omitted, Proposed ADR-017 was called Superseded, ADR-021 titles drifted from its body, and later ADRs were absent. | Make both documentation indexes mirror each body title and status and express conditional supersession without promoting a proposal. | Odysseus / this governance change | ADR bodies with Accepted or Superseded status remain immutable. | Index-to-body title/status comparison, unique-number check, and Markdown lint. |
| G-06 | Repository guidance attributed a binding evidence policy to Proposed ADR-014 and said a merge could end Proposed status. | Keep the evidence-integrity rule as explicit repository policy, identify ADR-014 as related Proposed rationale, and require a reviewed human status decision for acceptance. | Odysseus / this governance change | The Proposed ADR-014 body is unchanged; the policy is not represented as an Accepted ADR. | Targeted text search and protected-body comparison. |
| G-07 | Every canonical repository must eventually use the exact three-line `CLAUDE.md` compatibility pointer. | Verify the Odysseus pointer already matches; make no change in this tranche. Component pointers belong to their later repository PRs. | Odysseus verified; component owners pending | No generated shared `AGENTS.md` boilerplate. | Exact byte comparison for `CLAUDE.md`; downstream checks after ADR-022 acceptance. |
| G-08 | The approved modernization plan spans 15 component repositories and protected integration surfaces. | Keep component implementation blocked until ADR-022 is formally accepted. Use the immutable inventory below as Wave-0 evidence, then rebind each downstream PR to its current remote head and repeat hidden/tracked/loader/manifest/generator/test/consumer searches before editing or deleting. | Each named repository / later PR | Workflow edits, desired-state changes, live reconciliation, releases, and gitlink integration retain separate human approval. | Repository-specific tests, exact-head CI, Athena PR review, and truthful live evidence where applicable. |
| G-09 | Odysseus architecture guidance omitted `agentic/Athena` and assigned plugin/skill ownership to library-only Hephaestus, contrary to Accepted ADR-016. | Add Athena to the repository tree, component inventory, diagram, and focused component routing; assign plugin/skill distribution to Athena and Python library/automation to Hephaestus. | Odysseus / this governance change | Accepted ADR-016 remains byte-unchanged; no component content, plugin setting, or runtime is changed. | Inventory and ownership text search plus immutable-body comparison. |
| G-10 | Proposed ADR-020, the `hi/v1` schema descriptions, and the M0 task manifest described issue M0-2 as ratification or asserted unproved retry/exhaustion behavior; the manifest also carried four stale subject identifiers and their dependency references. | Record the checked-in schema and executable test as structural compatibility evidence; correct schema annotations, authored task descriptions, subject identifiers, and matching `blocked_by` references so neither M0-2 nor those artifacts accept ADR-013/020 or prove M1–M6 runtime behavior. | Odysseus / this governance change | Schema vocabulary, fields, required set, enums, defaults, and test semantics remain; workflow task IDs, titles, role/repository assignments, task count, model/default fields, and dependency graph remain even though the corrected subject/reference values are semantic manifest edits. | Exact source references, description-stripped schema equality, task/assignment/model/default counts, subject-reference reachability, dependency-graph equality, executable schema test, and targeted status-wording review. |
| G-11 | The plan protects accepted decisions, closed issues, raw evidence, frozen benchmark cohorts, and current Mnemosyne knowledge; it also forbids new instruction-size or prompt-text gates. | Preserve those bytes and classifications. Record an already-fixed item as a no-op rather than recreating it; never rewrite protected history to make the modernization appear complete. | Every repository / later PR | No accepted ADR body, closed issue body, raw evidence, frozen cohort, or current Mnemosyne corpus-body edit; no byte/token/word gate. | Exact-object/hash comparisons and path-classification inventory in each PR. |
| G-12 | Deletions and protected workflow edits can silently remove live consumers or security gates. | Before deletion, repeat hidden/tracked, loader/packager/generator/test, and organization-wide consumer searches at the bound SHA and remove consumers/exclusions atomically. Obtain human approval before every `.github/workflows/**` edit; remove only editorial prompt gates and repair false-green checks while retaining behavioral, parser, schema, security, evidence, and compatibility-pointer gates. | Owning repository / later PR | Live desired state additionally requires reconciler readback and operator approval. | Search transcript, classified deletion ledger, exact-head CI, and live receipts when state exists. |
| G-13 | Odysseus still has oversized/stale root guidance, duplicate milestone prose, unsafe legacy routing/completion paths, skill-install drift, and a hierarchy check that can falsely pass without submodules. Its skip-permissions section falsely described the host filesystem as inaccessible despite live workspace and credential/config mounts. | Correct the current exposure claim in this governance change without claiming Proposed ADR-025 is implemented. After ADR-022 acceptance, slim the remaining root contract; correct counts, status, fallback, runbook, onboarding, and command guidance; make Telemachy manifests the future M0–M6 description source; modernize templates; install only Athena skills into `.agent_brain/knowledge`; fail unavailable hierarchy checks truthfully; and harden the retained harnesses until retirement proof. | Odysseus / this governance change for exposure truth; later implementation PRs for the remainder | Do not rewrite closed issue bodies, accepted ADRs, canonical configs, defaults, or current harness behavior; legacy retirement waits for G-31. | Exact-base mount/environment/source comparison and targeted policy search now; focused docs/tool/harness tests, hostile payload/routing/verdict fixtures, protected-scope tests, exact CI, and terminal-evidence tests later. |
| G-14 | Athena has 17 public skill IDs but a 304-line generated 91-principle mirror, plugin-only broad `defaultPrompt`, long procedural root skills, and editorial validators. | Preserve `athena@Athena` and all 17 IDs; make each root skill a discriminating router, move detail to contextual resources/scripts, preserve strict review/worktree/evidence state machines, remove the mirror and broad default, and replace exact-prose checks with structural/package/reference/executable-workflow validation. | Athena / later PR and release | No `agents/openai.yaml`; workflow edits require approval; public IDs remain stable. | Plugin/package/reference validation, per-skill trigger and progressive-loading tests, state-machine tests, Athena PR review, and release evidence. |
| G-15 | Mnemosyne's root guidance pins models, five sections, size limits, and duplicate principles; the live corpus has 786 entries, 183 notes, and 318 histories at the bound SHA, including an Atlas delivery entry that remains a consumer under the preservation rule. | Slim only root/future-authoring guidance; preserve every corpus entry, note, and history byte-for-byte. Deprecate or override the Atlas guidance only in non-entry future guidance unless a separately authorized exception later changes the preservation decision. Retain metadata, provenance, duplicate, and link integrity. | Mnemosyne / later PR | Corpus tree `1e46f9fac1e46d0e0b16492a971e219d5d5de4fd` is immutable for this program; protected workflow edits require approval. | Corpus-tree hash, entry/note/history counts, metadata/provenance/link checks, consumer search, and exact-head CI. |
| G-16 | Hephaestus has a 934-line contract, stale settings/catalogs, recursive writing-standard injection, terse editorial prompt sentinels, fixed validation delegation, and no per-invocation Astra seam. Its latest head already removes branch-format enforcement from the documentation policy and adds behavioral policy tests. Its Athena contract manifest pins older advise/learn content and a stale marketplace PR-review path; executable prompt consumers remain internal at the rebound head. | Slim the contract and live prompt families around trusted operation, fenced context, authority, completion, and output; add one exact-literal Codex `gpt-6-astra` supplement per new or resumed invocation outside fences; preserve template names/overrides, parsers, schemas, tool scopes, state machines, bounds, Fleet build-artifact interfaces, exact branch-identity checks, and defaults; replace the remaining editorial prompt snapshots with semantic tests; update the Athena manifest only after the Athena release. | Hephaestus / later PR and release | No process-global model switch and no activation for empty/default, `astra`, or other models; release order is G-29. | Internal-consumer and rebound-delta inventory, new/resumed exact-once matrix, hostile-fence and authority tests, manifest hashes, existing parser/schema/state tests, exact-head CI, Athena review, and release artifact. |
| G-17 | Agamemnon drops optional `model`, accepts inconsistent scalar/array `programArgs`, documents PUT as replacement although PUT/PATCH both merge and downstream callers rely on merge, carries two issue-specific prompt artifacts and a self-consumed broken scaffolder, and has stale language/store/peer semantics. | Round-trip `model` through store/OpenAPI/Python client/reconciler/rollback/export; accept legacy scalar only at `myrmidons/v1`, normalize and always emit `string[]`; document merge semantics; remove the two prompt artifacts and scaffolder/docs; make Myrmidons the only template owner; transport task text unchanged as untrusted data. | Agamemnon / later PR | Preserve `hi/v1`, merge behavior, model/provider/lane/role defaults, and protected workflow until approved. | Store/API/client/reconcile/rollback/export round trips, scalar-normalization compatibility, merge tests, self/external scaffolder-consumer search, and exact-head CI. |
| G-18 | Telemachy has legacy settings, stale NATS/persistence and hook-bypass guidance, five unconsumed follow-ups plus an obsolete skill README, no canonical M0–M6 manifest set, and local versus Docker dispatch differs: Docker still calls removed `/v1/agents/docker` while explicit program/model fields are inconsistent. | Correct guidance and descriptions, remove verified obsolete artifacts, establish Telemachy manifests as the future M0–M6 description source, use Agamemnon's canonical endpoint in both modes, and forward explicit program/model fields consistently without changing defaults. | Telemachy / later PR | Protected workflow edits require approval; keep defaults and task text unchanged/untrusted. | Local/Docker parity tests, canonical-endpoint/field fixtures, manifest generation tests, consumer searches, exact-head CI, and Athena review. |
| G-19 | Myrmidons has a 455-line catalog contract, legacy per-skill settings, scalar-only `programArgs` schemas plus 55 authored scalar occurrences, editorial AGENTS checks, a stale PR template, and one-off authoring guidance. Exactly 43 active issue-8/22/69 agents (10/3/30) and three fleets remain; dynamic loaders consume them and live state was not queried. | Slim the contract; enable `athena@Athena`; accept legacy scalar then canonicalize and emit arrays; remove editorial checks while retaining structural/security behavior; repair the template and route future one-offs to Telemachy. Query the live reconciler and obtain operator approval; if inactive, hibernate/apply/verify in one PR, then delete in a second. Retain Myrmidons as sole template owner. | Myrmidons / later compatibility and two desired-state PRs | No manifest/fleet mutation from Git evidence alone; preserve `hi/v1`, model/lane/provider values and token order. | Pointer no-op record, schema/manifests/loader inventory, compatibility tests, live reconciliation receipts, operator approval, shutdown proof, deletion consumer search, exact-head CI, and Athena review. |
| G-20 | AchaeanFleet has a 252-line catalog contract, legacy per-skill settings, contradictory network/Eris/Pallas docs, workspace/mount/casing drift, unconsumed project variables, count drift, false-green local CI, bypass/force advice, and overbroad sidecar claims. `AGENT_PROGRAM` and `AGENT_HEADLESS_FLAG` have no runtime consumer; nine Dockerfile declarations remain and only protected CI inspects `AGENT_PROGRAM`. | Slim and route facts to Compose/HCL/pod/Dagger sources; enable `athena@Athena`; correct topology/workspace/count/sidecar guidance; repair false-green checks and unsafe advice; after workflow approval move the binary check to explicit versioned data and remove both image variables. | AchaeanFleet / later PR | Preserve current model assignments, host-mount limits, runtime topology until tested, and security gates. Protected workflow edits and the expired Trivy-exception failure require human security review; never extend dates blindly. | Pointer no-op, runtime-source and organization consumer searches, compose/config/build tests, false-green negative fixtures, run 34839980306 disposition, exact-head CI, and Athena review. |
| G-21 | Nestor has a 147-line catalog contract and legacy settings; it claims stale Telemachy/Keystone/BlazingMQ/`hi.pipeline` relationships, nats.c 3.12, and `0.0.0.0:8080`, while CMake selects nats.c 3.9.1 and code binds `127.0.0.1:8081`. CONTRIBUTING claims Conan instead of FetchContent; orphaned skills, API-doc-to-AGENTS routing, and bypass advice remain. | Slim the contract/settings; route API/NATS/security/release material to focused docs; correct build and interface facts; remove orphaned references and unsafe guidance; keep public API authority outside `AGENTS.md`. Repair Docker-publish permissions only with protected-workflow/external-package authority. | Nestor / later PR | Preserve real build/security gates. Current Required Checks are green; Docker Publish run 35255742133 fails GHCR `read_package`. | Pointer no-op, CMake/code/doc fact checks, interface tests, consumer searches, Required Checks, and separately authorized publish-run evidence. |
| G-22 | Argus has a 517-line catalog contract and legacy settings; read-only/no-application claims conflict with ACK/dispatch/application code, now including an opt-in three-read Fleet observation collector; scope/concurrency/protected paths and static catalogs drift; a cardinality reference is nonexistent; dashboard tests enforce editorial catalogs. Atlas dispatch/aggregate assets remain consumed by Odysseus launch paths, six PR templates, docs, and preserved Mnemosyne guidance. | Slim/correct the contract/settings and replace editorial tests with structural/behavior checks. Migrate every live Atlas-review consumer to the standard Hephaestus path before deleting scripts, charter, or links; preserve the Mnemosyne entry under G-15 and use a non-entry deprecation/override unless separately authorized. | Argus plus Odysseus/Mnemosyne / coordinated later PRs | No deletion while any consumer remains; workflow/template edits follow their own boundaries. | Pointer no-op, code/contract and rebound-delta review, organization-wide consumer search, replacement-path tests, deletion search, exact-head CI, and Athena review. |
| G-23 | Hermes has a 487-line catalog contract and legacy settings. It must retain async, subject, wire, configuration, lock, and HMAC invariants, but guidance claims synchronous Agamemnon/stateless behavior, routes public API comments through AGENTS, includes bypass advice, and its Markdown/local suppression checks can pass after missing tools or lint failures. Two distinct Accepted ADR-004 bodies exist. | Slim the contract/settings, make README/OpenAPI the interface authority, remove contradictions/speculation/bypass advice, retain invariants, repair false-green checks, and resolve ADR index compatibility without editing either Accepted body. | Hermes / later PR | No Accepted ADR body edit, invariant/wire/auth weakening, or protected workflow edit without approval. | Pointer no-op, interface/source fact review, immutable ADR hashes, Markdown/suppression negative fixtures, build/security tests, exact-head CI, and Athena review. |
| G-24 | Charybdis has a 269-line contract and legacy per-skill settings; its roadmap marks completed work pending. Its externally consumed contract is Agamemnon `/v1/chaos/*`, despite stale Agamemnon docs claiming injection is not wired. | Slim guidance/settings, enable `athena@Athena`, correct roadmap and cross-repo interface facts, and retain test-subject isolation, Agamemnon-only fault injection, guaranteed cleanup, explicit chaos authorization, and separate ASAN/TSAN runs. | Charybdis plus Agamemnon docs / later PRs | No broader chaos authority, combined sanitizer shortcut, or endpoint/default change. | Pointer no-op, Agamemnon source/client/OpenAPI/tests and Odysseus E2E consumer inventory, fault-boundary/cleanup tests, separate sanitizer CI, exact-head CI, and Athena review. |
| G-25 | Keystone has a 405-line contract and legacy settings, an obsolete 34-agent hierarchy, agent-only scaffolding, unconsumed `setup-notes.sh`, stale `.clinerules`, and unique C++ concurrency/security rules mixed into agent guidance. | Enable `athena@Athena`; move unique current rules into focused docs; remove verified obsolete hierarchy/scaffolding/notes; replace `.clinerules` with a minimal `AGENTS.md` compatibility pointer; retarget documentation checks. | Keystone / later PR | Preserve current runtime and security behavior; protected-check edits require approval. | Pointer no-op, rule-preservation review, hidden/loader/consumer search, pointer/doc tests, build/security tests, exact-head CI, and Athena review. |
| G-26 | Proteus has a 310-line contract and legacy settings, four unconsumed transient files (`.claude-followup-103.json`, `.claude-followup-6.md`, `.claude-followup-82.json`, `.followup-94.json`), and stale claims about closed issues, pytest, gitleaks, dispatch, CHANGELOG, and mandatory full validation after every edit. | Enable `athena@Athena`; delete the four verified transient artifacts, correct facts, and use path-scoped affected checks while preserving hard security denies. | Proteus / later PR | No weakened gitleaks/security boundary, fabricated closed-issue state, or protected-workflow edit without approval. | Pointer no-op, hidden/organization consumer search, affected-check routing tests, security scans, build/tests, exact-head CI, and Athena review. |
| G-27 | Odyssey has 29 roles, overlapping prompts, stale Mojo/MCP/model guidance, unconditional auto-merge, widespread chief-architect coupling to `plan_issues.py`, host permission bypass, and obsolete marketplace/migration wiring. | Retain and modernize exactly nine named roles and delete the other 20 only after consumer migration; refactor chief-architect atomically with `plan_issues.py`; modernize prompts/commands/resources/hooks/skills; remove bypass and fail closed under explicit read-only policy; remove verified nonexistent/obsolete wiring. | Odyssey / coordinated later PRs | No automatic merge without exact review/CI authority; preserve Scylla frozen fixtures and Mnemosyne corpus references; workflow edits require approval. | Exact 29→9 role/consumer inventory, planning atomicity tests, tool-policy negatives, migration-script/tests/docs inventory, prompt semantics, exact-head CI, and Athena review. |
| G-28 | Scylla has dense operational/judge prompts; its Codex adapter uses stale `codex --model ... --quiet` without `exec`, another path silently defaults to Claude despite explicit provider configuration, 40 unregistered legacy skills remain, and commit `acf03e5e` deleted a benchmark cohort. | Slim eight operational agents and judge/tier prompts without changing semantics; use `codex exec` and wire explicit provider with no model default; restore the exact parent `f2f6727` tree `5e1f7a46…` as a 119-file/82-SKILL legacy cohort with provenance/hashes; update composer/counts; migrate unique live rules then delete the 40 unregistered skills. | Scylla / later PRs | Never alter blocks, agents, subtests, tiers, `docs/arxiv/dryrun/raw/**`, scoring, frozen cohort bytes, or immutable external consumers; do not create an Astra cohort. | Historical Git-object hashes and counts, composer/generator checks, adapter/provider tests, scoring/JSON/cleanup tests, deletion/consumer searches, exact-head CI, and Athena review. |
| G-29 | Consumer settings cannot safely target unreleased canonical prompt/skill providers. | Release Athena first, then Hephaestus; merge consumer settings only after both required releases are available. | Athena, Hephaestus, then consumers | No speculative release version or default/provider change. | Published immutable release artifacts, dependency resolution, consumer exact-head CI, and recorded ordering. |
| G-30 | Cross-repository pins are a separate integration effect. | Land child PRs and canonical releases first; then obtain explicit integration approval and update all Odysseus gitlinks in one integration PR. | Odysseus integration owner / later PR | No gitlink or `.gitmodules` change in governance or child PRs. | Exact child merge/release SHAs, clean submodule status, integration tests, review, and CI. |
| G-31 | Legacy myrmidon retirement requires real integrated mesh parity, not unit or simulated equivalence. | On exact integrated pins, prove routing, three-heavy-worker concurrency, timeouts, isolation, tool scopes, retry/restart, issue binding, branch/PR lifecycle, and truthful receipts; run one real ADR-020 M4 mesh-only dogfood issue; only then remove both harnesses in a separate human-reviewed PR while keeping Hephaestus's supported single-device fallback. | Odysseus / later proof and retirement PRs | No harness retirement from proposal acceptance, unit tests, or simulated parity alone. | Exact-pin integration record, real dogfood output or truthful failure, separate review, retirement consumer search, and exact-head CI. |
| G-32 | The user requires every PR to pass Athena review and CI/CD before merge and asked that found issues be repaired. | Bind each PR to its immutable head; repair every in-scope NO-GO or CI finding, rerun exact-head review/checks, enable auto-merge only after delivered GO and required green CI, and report external/protected blockers truthfully. Limit heavy validation to three jobs and rely on hosted CI where local platform capabilities such as Tailscale are unavailable. | Every later PR owner | Review is not replaced by local validation; no bypass, force push, fabricated evidence, or merge on red/unknown required checks. | One exact-head Athena review state per round, required CI/CD result, merge receipt, and blocker record where authority is missing. |
| G-33 | The root Grafana credential gate blindly opened deleted tracked paths, could skip symlink/type replacements, recognized only ASCII YAML anchors, and ignored tracked Compose `env_file` inputs that can enable anonymous Grafana access. | Keep the tracked-tree boundary and no-follow reader; skip only a missing tracked leaf, reject every non-regular/unreadable/replaced prefix, detect dotted/Unicode/flow YAML indirection, and safely bind literal tracked Grafana env-file targets. Dynamic, empty, missing, untracked, symlinked, inline, or escaping targets fail closed. | Odysseus / this governance change | No credential-warning or e2e-only anonymous-access policy weakening, protected workflow edit, or untracked/submodule scan expansion. | Embedded GREEN 61/61 self-test, live credential gate, Python compile, full `pixi run ci`, exact-source review, and hosted CI. |
| G-34 | The document-field drift gate passed after `grep` failed to open a tracked path deleted in the worktree; a naive existence filter would similarly skip a tracked symlink/type replacement, a later parent replacement could evade an initial check, and newline-delimited inventory would split a legal unusual path. | Use the shared NUL-safe tracked scanner; skip only an absent leaf, open every prefix no-follow, require a stable regular descriptor, re-resolve every opened prefix after read, and classify every other failure as unavailable. | Odysseus / this governance change | Preserve the first-party tracked-doc boundary and deprecated-field behavior; no protected workflow edit. | Embedded GREEN 7/7 scanner self-test, `tests/test-doc-field-drift.sh`, live gate, full `pixi run ci`, exact-source review, and hosted CI. |

## Detailed repository dispositions

The rollups above define sequencing. Each row below records one verified
finding or a group whose action, owner, boundary, and evidence are identical.
`Already fixed` means no edit should be recreated. `Approval blocked` and
`live-state blocked` are not implementation completion.

| ID | State and finding | Chosen action | Owner | Protected boundary | Verification evidence |
|---|---|---|---|---|---|
| D-01 | Already fixed: Odysseus, Hephaestus, Agamemnon, Telemachy, Myrmidons, AchaeanFleet, Nestor, Argus, Hermes, Charybdis, Keystone, Proteus, Odyssey, and Scylla already have the exact three-line pointer. | Record no-op; preserve exact bytes. | Named repositories | No gratuitous pointer rewrite. | Byte comparison to Odysseus `CLAUDE.md`; shared SHA-256 for the 13 component pointers is `b5f5aacf431278b21fa0acb6364ea00e88a59d09b6add38ffc63cc73efe8618f`. |
| D-02 | Actionable: Athena and Mnemosyne have noncanonical pointer text. | Replace each with the exact three-line pointer after ADR-022 acceptance. | Athena and Mnemosyne | `AGENTS.md` remains repository-specific. | Exact byte comparison and pointer-link check. |
| D-03 | Already fixed: Odysseus, Athena, and Mnemosyne already enable `athena@Athena`. | Record no-op; preserve repository-specific hooks and denies. | Named repositories | Do not add `agents/openai.yaml`. | Settings parse and plugin-identity search. |
| D-04 | Actionable: the other 13 component settings enumerate legacy per-skill keys. | Enable `athena@Athena` only after its release; preserve all repository-specific hooks, permissions, and denies. | Each component / later PR | Hephaestus/Odyssey safety-net behavior and Scylla plan-export behavior remain. | Settings parse, before/after hook/deny comparison, exact-head CI. |
| D-05 | Already fixed: no canonical repository contains `agents/openai.yaml`. | Record no-op; do not add one. | All 16 repositories | Provider-specific guidance belongs only at the invocation seam. | Hidden tracked-tree search at each bound SHA. |
| NATS-01 | Current defect: spoke `remote.account` selects a local account, hub leaf auth is account-unbound and would accept into `$G`, and v2.10.22 rejects the scalar `token`; current CI parses only the hub while the Python validator is delimiter-only. | Record the truth in Proposed ADR-024 and require executable old-shape `$G` and scalar-token regression fixtures before any migration. | Odysseus NATS implementation owner | No canonical config or workflow edit in this tranche. | Exact-base config, parser/auth source, CI, and validator anchors; later real server tests. |
| NATS-02 | Version/security drift: executable sites select v2.10.22, v2.10.24, digest-pinned 2.10, mutable tags, and ambient binaries; official v2.12 fix records conflict and v2.12 is outside current support. | Inventory every executable site; select only one exact release in a vendor-supported minor after a fresh complete official advisory and upgrade review. Treat 2.12.12/2.14.3 only as historical branch floors. | Odysseus NATS implementation owner | No evergreen numeric pin or current pin change in governance. | Pin-site inventory, signed advisory/upgrade manifest, digest and live version readback. |
| NATS-03 | Ownership drift: Agamemnon currently reconciles streams whose accepted semantic owner is Hermes; exact API subjects do not constrain body-selected subjects/sources/mirrors/transforms/republish or consumer filters/delivery. | Hermes owns agents/tasks/deadletter manifests and Agamemnon the other four; all mutations use a complete-body-validating fixed helper while ordinary roles retain only exact info/pull/ack/runtime grants. | Hermes and Agamemnon | Preserve accepted subjects/names and runtime client needs. | Canonical manifests, body mutation negatives, owner mismatch denial, role permission tests. |
| NATS-04 | Monitoring classification drift: SYS covers server/system monitoring, while JetStream advisories are account-scoped and current unauthenticated HTTP monitoring would bypass the local-account boundary. | Keep SYS local; use one fixed non-forwarding node-local SYS collector that exports only a closed sanitized metric schema to Argus over verified host-local mTLS; if needed, define one least-privilege local HOMERIC advisory observer. Disable remotely exposed unauthenticated monitoring HTTP. | NATS implementation owner | No remote/shared Argus SYS identity, system request authority, raw payload/URL, or application back door. | Runtime interest/account readback, collector identity/schema/cardinality/staleness tests, HTTP exposure and cross-account denial tests. |
| NATS-05 | Migration authority risk: one broad credential could cross old source accounts and target; snapshot delivery subjects and bodies carry authority, while restore returns a server-selected dynamic subject. | Issue one expiring identity per old source account and a separate target request identity; load exactly one per helper process/operation. Snapshot uses a manifest-derived delivery subject. Bind the exact server-returned `$JS.SNAPSHOT.RESTORE.<stream>.<nuid>` to its authenticated pending request and issue a second identity whose sole publish grant is that subject. Independently revoke each identity. | NATS migration operator | No temporary operator-authored source/target application bridge, caller-selected restore subject, wildcard transfer grant, or two-plane credential lifetime. | Positive per-plane capture/restore; exact response/session/stream/NUID binding; neighboring-account/SYS/premature-target/foreign-body/foreign-session/replay/expiry denial. |
| NATS-06 | State-evidence gap: INFO/FastState and public ConsumerInfo cannot expose complete persisted pending/timestamp and redelivery maps, and snapshots/stores contain confidential application state. | Use two independent exact-release/schema-hashed decoders: checked archive plus read-only copy of the fully stopped store. Require byte equality; use INFO only to corroborate exposed fields. Protect complete source/target stores, bridge archives, backups, snapshots, and copy-on-write descendants owner-only, no-follow, encrypted with separate keys/readers/quotas/retention and verified deletion. | NATS migration operator | No inferred hidden state or payload in CI/log/repository; recovery windows remain explicit. | Unknown-field/archive/store fixtures, decoder disagreement negatives, full-state equality, zero-writer proof, storage confidentiality/capacity/crash/deletion tests. |
| NATS-07 | Topology inference: one hub store and no spoke duplicate is desired, while NATS-managed JetStream system export/import/domain plumbing still exists. | Label exactly-once storage as an inference to prove; forbid operator-authored application/source-target sharing but inventory and permit only exact-release managed plumbing. | NATS implementation owner | No claim that config prose proves runtime topology. | Sequence/ack/store readbacks and runtime export/import/domain inventory. |
| NATS-08 | Keystone compatibility defect: Accepted ADR-005 names `keystone-dag`, but the exact-pinned task listener leaves its `keystone-daemon` configuration default unwired, requests a server-assigned consumer, accepts foreign-prefix or suffixed subjects with at least five tokens, acknowledges task verbs outside ADR-005, and acknowledges terminal events after a print-only callback rather than an observable DAG effect. The transparent bridge similarly leaves `keystone-bridge` unwired and server-assigned. It violates ADR-002 by mixing binary point-to-point messages with JSON lifecycle events on `hi.agents.>`, republishes inbound nonlocal deliveries into a wildcard loop, and can leave outbound publishing active after attach failure. | Before RP0, explicitly bind and reuse `keystone-dag`; require the exact task prefix, five-token arity, ADR-005 verb set, and a durable idempotent DAG effect receipt before acknowledgment; map quiesced stream, delivery, acknowledgment, pending, and effect state to an exact canonical start boundary; then classify/drain/delete server-assigned task consumers only after no-loss/bounded-duplicate proof. If the effect path is absent, RP0 stops; retirement requires a separate Accepted ADR superseding ADR-005 and complete state disposition. Reconcile and retire the bridge before RP0 by classifying every server-assigned bridge consumer and incompatible binary record, resolving or source-locally archiving every pending or unacknowledged message, then deleting every exact binary source sequence under a reviewed manifest whose gaps or tombstones are preserved in the RP0 state. Any future replacement is post-RP0 and requires a separate Accepted ADR and later frozen migration defining a distinct subject/stream and payload schema, routing/cardinality/filter ownership, state handoff, poison/dead-letter handling, local-only no-loop delivery, atomic activation, health, and exact grants. | Keystone and NATS migration owners | Do not rename or silently retire `keystone-dag`, accept a foreign-prefix, suffixed, or out-of-contract task verb, acknowledge a print-only effect, infer durable state from unused strings, use an ambient delivery default or guessed start, migrate or delete unclassified consumers or records, discard unresolved bridge deliveries, copy an incompatible bridge payload to the target, treat ADR-005 as bridge authority, reuse `homeric-agents`, grant blanket `hi.agents.>`, retain any bridge or replacement at RP0, republish an inbound delivery, leave a partial-active bridge after failure, negatively acknowledge poison forever, or advance RP0 while either path is unresolved. | Exact source anchors; full live consumer/process inventory; exact task prefix, arity, and verb positives and negatives; sequence-to-effect/start mapping; committed-effect-before-ack, no-loss/bounded-duplicate, task restart reuse, bridge-consumer/record disposition, exact deletion-manifest-to-gap/tombstone match, zero incompatible target payload, no-recreation, seven-stream inventory, canonical manifest, and ACL readback. |
| NATS-09 | Telemachy's exact-pinned registrar publishes `hi.pipeline.epic.{epic_key}.registered` and Agamemnon subscribes to that grammar, while its monitor only subscribes to bound-team task events. The draft ACL instead granted unspecified task publication and omitted the live pipeline publication. | Grant Telemachy publish only on `hi.pipeline.epic.*.registered` with registrar-produced single-token keys; retain only its exact bound-team task subscription and reply inboxes; deny direct task publication and every sibling pipeline subject. | Telemachy and NATS migration owners | Do not convert Proposed ADR-013 task-publish behavior into current authority or use a broad `hi.pipeline.>` grant. | Exact source anchors; positive registrar publish and Agamemnon receipt; multi-token/empty-key, sibling-pipeline, other-team-subscribe, and all-task-publish negatives; rendered ACL and runtime authorization readback. |
| NATS-10 | The draft version-first phase said to preserve the old topology while repairing rejected leaf syntax, but the old hub listener is account-unbound; a syntax-only repair would connect all four spoke accounts to hub `$G`. | Before any binary replacement, quiesce traffic and choose either a fully disabled, zero-session leaf phase or four distinct hub leaf identities explicitly mapped to the four old named accounts. Forbid `$G`, syntax-only, asymmetric, and partial intermediate states; require live account/traffic readback and full-config rollback. | NATS implementation and migration operators | No checked-in NATS config, executable pin, credential, workflow, or live deployment changes in this governance tranche. | Both intermediate-mode fixtures, operator/quiescence record, positive/negative account readbacks, zero-session proof, rollback rehearsal, exact digest readback, and hosted integration CI; config parse alone is insufficient. |
| NATS-11 | The rollback design bound server/account state but not the underlying source and target JetStream stores; every current config names `/var/lib/nats/jetstream`, so a same-host target could reuse or mutate the rollback store, and deletion could race live target writers. | Before manifest freeze, allocate and no-follow bind an empty, pristine target with distinct volume/device, filesystem/mount, root-inode, and allocation identities; keep it inaccessible/unwritten until source revocation/sealing. Rollback first revokes target/canary identities, inhibits restart, reaps all target/helper/canary processes, and proves zero sessions/push bindings/pull waiters/writable handles/mappings before exact target/key deletion and source reopen. | NATS implementation and migration operators | No current store, checked-in config, or live data is changed by this proposal. RP0 is not rollback evidence if stores alias, share writable backing, use placeholders, mutate the frozen binding, or cleanup races a live writer. | Alias/link/reflink/bind/shared-device and target-substitution negatives, manifest-freeze/CAS crash tests, process mount/FD readbacks, ordered stop/revoke/zero-handle barrier, deletion receipt, target-write/delete source-integrity checks, source reopen, RP0 replay, and hosted integration CI. |
| NATS-12 | The task repair rejected malformed subjects and verbs but left their terminal disposition undefined; exact-pinned Keystone NAKs some malformed records forever and ACK-drops unknown verbs. | Before `AckTerm`, commit an idempotent durable local invalid-task receipt keyed by stream/sequence/consumer with fixed reason and digests. Reconcile crash windows without callback; pause/quiesce on ledger or readback failure; bind every invalid sequence and outcome into RP0. | Keystone and NATS migration owners | No silent ACK loss, raw-payload receipt, unbounded NAK loop, callback invocation, or RP0 advance with an unresolved invalid record. | Prefix/arity/verb fixtures, receipt-before-term ordering, both crash windows, restart reconciliation, pending/floor readback, no-callback/no-loss/no-loop proof, and RP0 ledger checkpoint. |
| NATS-13 | The draft made the first observed ordinary publish/ACK the point of no return after reopening admission, left `cutover_committed` without an independent durable authority, and could start producers before exact consumers were bound. | Use an independent two-copy root-owned CAS ledger keyed by change/manifest/generation with fenced states `prepared`→`committed`→`consumers_ready`→`producers_admitted`. `committed` is the point of no return. After it, keep producers denied, start and verify exact ordinary consumers/effect sinks first, then admit producers; ambiguity always selects forward recovery. | NATS migration operator | The helper lease is not the cutover ledger; no RP0 rollback after uncertain/confirmed commit and no producer-first admission. | Foreign-writer/fence/corruption/divergence/lost-response tests, crash injection around every transition, durable readback, precommit rollback, consumer-first readiness, postcommit forward recovery, and delivery-side-effect-before-ACK fixture. |
| NATS-14 | The version-first phase promised old-binary rollback after a candidate may have mutated the only JetStream store, but its RP0 is captured only later and no on-disk downgrade authority was bound. | Before replacement, capture a quiesced immutable pre-upgrade recovery point. On an exact copy, prove either complete bidirectional old↔candidate compatibility or protected backup restoration to a distinct store reopened by the old binary; otherwise do not replace production. | NATS implementation and migration operators | A candidate-touched store and a start-only downgrade are not rollback evidence; backup handling inherits the migration confidentiality/integrity controls. | Exact-release downgrade or restore rehearsal, complete state and byte/tree comparison, separate-store identity, failure injection, capacity/reader/key/deletion controls, and blocked-replacement negative. |
| NATS-15 | The draft drained retained durable consumers but merely stopped Core subscriptions, whose buffered callbacks and external effects have no server acknowledgment floor or replay boundary. | Under producer deny, drain each Core path, bind every exact source sequence to an idempotent effect receipt or reviewed replay, and create exact named durables at the first provably uneffected sequence before RP0. Preserve or transform their full state through restore; forbid guessed delivery starts. | Nestor, Agamemnon, Telemachy, and NATS migration owners | Do not infer Core completion from stream stability, use DeliverNew/All/current-last/wall-clock guesses, or admit producers before restored named consumers are ready. | Producer/drain ordering, sequence/effect ledger, exact replay, durable start/config/state, merge transform, crash/in-flight/delayed/lost-response negatives, restart reconciliation, and RP0 binding. |
| NATS-16 | The frozen manifest required a target server ID before first start, while the ID is an ephemeral fact that does not yet exist and cannot mutate the frozen manifest later. | Freeze target node/config/TLS/endpoint/store and activation generation plus a signed activation-receipt link slot. Record the first observed server ID only in that linked receipt and bind each later session/restart to the same stable facts. | NATS migration operator | No invented pre-start ID, mutable manifest, or ID-only store authority. | First-start/restart receipt tests; wrong store/config/TLS/endpoint/generation/ID negatives. |
| NATS-17 | `MaxAge`, `InactiveThreshold`, and credential/certificate expiry can mutate or delete state during otherwise quiet migration; quiescence does not freeze wall time. | Bind independently attested clocks, maximum skew, a monotonic budget, selected-release timer semantics, and minimum remaining lifetime at source freeze, snapshot, target start, RP1, and commit. Wait and recapture or approve an exact timer delta; never use an unrecorded heartbeat. | NATS migration operator | No silent expiry/deletion, clock assumption, or near-expiry auth at cutover. | Skew/clock-jump/budget/near-expiry fixtures, deterministic expiry transform, repeated lifetime readbacks. |
| NATS-18 | Precommit canaries could ACK production durables, evict real messages, or trigger GitHub/task/DAG/Loki effects before the rollback point. | Use non-consuming auth probes or an isolated shadow consumer and disposable idempotent sink. Append only with proved non-evicting headroom; sealed/full/discard-old state uses read-only or isolated-store proof. Drain/revoke canaries and require zero pull waiters/callbacks/effects and `PushBound=false` before RP1. | NATS migration and component owners | No production durable ACK, backlog mutation/eviction, or remote production effect from a canary. | Full/sealed/discard/headroom negatives; sandbox sink receipts; GitHub/task/DAG/Loki credential denial; final drain/push-bound readback. |
| NATS-19 | Accepted ADR-006's universal Keystone-transport language conflicts with retiring the transparent bridge, while ADR-002 reserves point-to-point messaging to AMP; the draft also treated mandatory verified TLS as a separate control. | If accepted, ADR-024 explicitly supersedes only ADR-006's conflicting universal-transport/all-traffic clauses, preserves ai-maestro removal and ADR-002/005, uses direct least-privilege NATS clients for infrastructure events, and makes authenticated verified TLS mandatory. | Architecture and NATS owners | No silent accepted-ADR contradiction, plaintext credential-bearing connection, or TLS deferral. | Relationship/index review, transport-role matrix, TLS/plaintext/SAN/expiry/secret-log tests. |
| NATS-20 | Workers and legacy harnesses directly published `hi.tasks` despite Hermes' accepted envelope ownership; pinned Myrmidons and optional Scylla paths create/bind broad state, ACK after failed or swallowed effects, and log raw endpoints. | Deny worker/legacy direct `hi.tasks`; route exact result records through reviewed Hermes ingress. Repair or retire pinned `hello-myrmidon` and optional Scylla with exact bind/domain/TLS, expected PubAcks, durable intents/results/effects before source ACK, poison ledgers, ambiguity reconciliation, and restart absence proof. | Hermes, Myrmidons, Scylla, and NATS owners | No stream creation by ordinary workers, direct canonical task envelope, silent ACK, unbounded poison retry, or default-disabled retirement assumption. | Exact-pin anchors, ACL negatives, PubAck/effect/receipt crash matrix, loader/restart tests, raw-URL sentinel tests. |
| CB-01 | Current-state defect: single harness mounts Claude/GitHub config and weakens modes; multi also injects `ANTHROPIC_API_KEY`; both use keep-id/mesh networking and candidate Git/GitHub effects; no host broker exists. | Proposed ADR-025 defines a later atomic cutover that removes every credential/config mount, injection, reusable token, and world-readable mutation and never reinserts credentials on rollback. | Odysseus harness owner | No runtime/default/credential change in governance. | Exact-base fixtures, inspect/process/mount/env/FD negatives, mutually exclusive path tests. |
| CB-02 | Egress gap: a `--network none` candidate cannot fetch dependencies or perform required forge delivery. | Host prefetcher/resolver supplies digest-bound inputs before launch; lock changes terminate/checkpoint and start a new authorized session; an exact manifest-bound forge adapter performs only approved post-review effects. | Odysseus harness owner | Candidate text cannot authorize egress or remote writes. | Hostile Git/config/hook/filter/helper/transport fixtures, tree/parent revalidation, effect idempotency/readback. |
| CB-03 | Filesystem/userns risk: keep-id maps the service identity and rootless host idmapped mounts are not a reliable selected boundary. | Select `--userns=auto:size=65536`, reject service IDs, use read-only image plus bounded private tmpfs, and transfer canonical archives through owner-only no-follow staging/quarantine with independent gate/host verification. | Odysseus harness owner | No host workspace/state bind, `:U`, idmap, host/keep-id, or ambient map. | Complete map/NS_GET_USERNS proof, mountinfo/inspect, hostile archive/copy and ownership tests. |
| CB-04 | Listener-transfer risk: namespace, ancillary, FD, capability, and crash ambiguity can leak or recreate authority. | Use a fixed single-thread launcher, exact netns/userns owner proof, one high-port loopback listener, authenticated `SOCK_SEQPACKET`/one `SCM_RIGHTS`, CLOEXEC everywhere, strict bounded ancillary parsing, post-drop host inspection, then launcher exit. | Odysseus harness owner | No host setns, routable listener, Docker/remote fallback, fork/exec, or interrupted-runtime resume; a later authorized CLI turn may restore only its separately validated session-state archive. | SO_NETNS_COOKIE/NSFS, truncation/unknown ancillary/FD leak, cap/no_new_privs/descendant/deadline tests. |
| CB-05 | Gate/OCI risk: `--preserve-fds` requires atomic `podman run`; intermediaries such as conmon can retain FD3; release IPC and credentials must be exact across namespaces. | Precommit `agent_run_armed`, bind the discovered waiting gate, enumerate start-helper/Podman/runtime/conmon/gate/supervisor FD topology, and use receiver-relative SCM credentials plus HELLO→ARM→READY→RELEASE→ACK→EXIT→EXIT_ACK. | Odysseus harness owner | Candidate never inherits FD3/nonce/control; selected exact runtime stack must prove its topology. | Exact-version FD-table tests, translated/overflow credential negatives, replay/order/deadline/peer tests. |
| CB-06 | Lifecycle risk: live pidfds/nsfds/FDs cannot be durable authority, reboot enables PID reuse, controller/supervisor/broker death can orphan work, and a provider request already emitted at death cannot be assumed rolled back. | Durable identity is boot ID+PID+start+executable/namespaces/maps; reacquire only after same-boot proof. Make the broker a non-daemon root-manager child with direct controller, supervisor, and manager lifelines plus parent-death kill; use controlled STOP/ACK and committed manager `STOP_ACTOR` escalation while the supervisor pidfd remains identity/wait evidence. Close authority after bounded lifeline detection and classify any request that may cross the poll/write race as uncertain for independent reconciliation. | Odysseus harness owner | Never recreate a listener, resume/re-run an interrupted candidate, silently replay a provider request, claim synchronous no-I/O-after-death or rollback/no effect from process death, or signal a reused/cross-UID PID; authorized future CLI session continuation is a separate validated archive. | Crash injection before/during/after provider issue at every phase, lifeline/poll-write/parent-race and manager escalation fixtures, broker worker/descendant/FD absence, uncertain-effect reconciliation, reboot/PID-reuse checks, exact cleanup readbacks, and receipt-last retirement. |
| CB-07 | Oracle defect: connection refusal alone cannot prove namespace isolation because loopback starts down and a second namespace can bind the same numeric endpoint. | Bring/read back loopback in a second disjoint pod, prove local self-connect, run both no-listener and decoy-listener trials with a public synthetic probe nonce, and require unchanged bound host-broker/provider counters before any operational token exists. | Odysseus harness owner | The probe nonce is nonsecret health-only input and is destroyed before the candidate-inaccessible operational token is generated; provider effect remains host-only. | Loopback flags/routes/netns/cookie, self-connect, decoy protocol denial, broker counter/readback tests. |
| CB-08 | Evidence leakage risk: raw frames, nonces/verifiers, tokens, provider/signing data, or untrusted stdout could enter durable receipts. | Persist only specified redacted fields, canonical redacted-frame/event-list digests, validation booleans, fixed errors, and the one non-authorizing broker-token digest until cleanup; zero secret-bearing in-memory transcript state and remove that digest with live authority. | Odysseus harness owner | No raw secret bytes, nonce-derived verifier, or reusable secret-derived durable authority. | Sentinel absence across logs/receipts/artifacts on success and every failure path. |
| CB-09 | Candidate privilege-drop risk: changing only UID/GID can preserve inherited supplementary groups and their file, socket, or signal authority. | Read and bind `/proc/self/setgroups`: in `allow` mode call `setgroups(0, NULL)` while authority remains; in `deny` mode make no forbidden call. In both modes require the group vector exactly empty before candidate GID/UID, capability clearing, `no_new_privs`, seccomp, and exec. | Odysseus isolated-harness owner | No candidate keeps an inherited group or `CAP_SETGID`; portable emulation is not Linux proof. | Both allow/deny branches, hostile inherited-group fixture, `/proc/<pid>/status` empty `Groups:` readback for candidate and descendants, group-only sentinel denials, capability/readback tests, and exact-head rootless-Podman Linux CI. |
| CB-10 | Forge/merge authority risk: a comment, label, approval, or ordinary auto-merge setting can become stale; a write-authorized push may not disable auto-merge, head-only merge has no expected-base predicate, and queue/check/review state can drift after readiness. | Stop the pre-review adapter after PR creation. Accept only an authentic terminal Athena `COMMENT` carrier bound to source head and base tip, with its publication anchor, ordered terminal ledger, expected actor/app identities, exclusive GO state, zero pending events/threads, and green exact-head checks. Merge only through an atomic source-head+base-tip queue/merge group whose result receives required checks; otherwise stop for human action. Any drift starts a new exchange; independently verify the admitted pair, merge result, commit, parents, tree, PR, and receipts. | Odysseus forge/delivery owner | Candidate text and stale GitHub state never grant merge authority; no standing ordinary auto-merge across write-authorized pushes and no head-only REST merge. | Spoof/chain/actor/app/label/thread/head/base-mutation negatives, absent-primitive stop, merge-group check identity, queue cancellation/reconciliation, positive terminal exchange, and post-merge readback. |
| CB-11 | Broker-token authority was named but had no generation or authenticated host→worker/gate delivery path after the listener receipt existed; implementers would have to invent an env, argv, archive, or frame channel. | Generate 256 random bits in the supervisor only after `listener_bound`; arm the exact worker once over its authenticated control socket, send the same secret once in FD3 `ARM`, have the trusted gate create the exact owner-only tmpfs file, and keep broker admission disabled until `gate_bound` plus `BROKER_ENABLE`. Zero transient copies and persist only the one non-authorizing digest/redacted frames until cleanup. | Odysseus isolated-harness owner | No token in archive, argv, environment, host bind, candidate output, or durable bytes; only the supervisor, exact worker, and trusted gate may observe it. The candidate cannot read or traverse to the token file. | Arm/enable ordering, byte/digest equality across trusted peers, peer identity, wrong-binding/deadline/replay negatives, candidate file-access denial, crash cleanup, later-invocation denial, and sentinel absence. |
| CB-12 | `receipt_retired` was described as removing the sole lifecycle receipt, erasing the replay/idempotency barrier after cleanup. | Retire live authority but retain the immutable terminal receipt through all remote/audit windows; atomically replace it only with a minimal append-only permanent consumed-operation tombstone. Operation IDs are globally nonreusable and missing/corrupt barriers fail closed. | Odysseus isolated-harness and forge owners | Never delete the last terminal/replay proof or treat a completed operation ID as new. | Same-operation replay after cleanup/restart, receipt→tombstone CAS/crash tests, retention/readback, corrupt/missing barrier negatives, and zero duplicate local/provider/forge effects. |
| CB-13 | Candidate output used an archive where absence could mean either deletion or truncation, so legitimate delete/rename work was ambiguous and omission could become authority. | Bind the complete allowed-path universe to the input tree and require one explicit `present` or `deleted` record per path; bind bytes/type/mode/digest for present, no bytes for deleted, and represent rename only as delete+add. Reconstruct independently before forge effects. | Odysseus isolated-harness and forge owners | No inferred deletion, unbound path, duplicate/missing state, archive truncation, or candidate-selected host path. | Create/modify/delete/rename positives; omission, duplicate/conflict, out-of-universe, missing/extra bytes, byte-bearing tombstone, type/mode/digest, truncation, and path-escape negatives. |
| CB-14 | Gate fork safety was undefined even though PID 1 parses protocol data, serves HTTP/SSE, handles archives, and later forks an admission child; a helper thread could leave allocator locks or secret-bearing stacks in the child. | Require the gate to remain a one-thread event loop, deny thread creation, and read back exactly one kernel task immediately before the sole fork; a second thread or unavailable proof fails before a child or work exists. | Odysseus isolated-harness owner | `MADV_DONTFORK` alone is not thread/fork safety, and no library helper thread is grandfathered. | Hostile background-thread parser/archive/library fixtures, exact task-count readback, and no-child/no-effect negatives. |
| CB-15 | Credential-bearing source prefetch, dependency resolution, and session-state storage were described as ambient host services outside the root manager's closed actor and cleanup model. | Add distinct one-operation fixed actors for source prefetcher, dependency resolver, and session-state service with exact parent/UID/GID/executable/LSM/seccomp/cgroup/lifeline/ledger bindings, operation-scoped credentials/keys, and the same crash/replay/STOP_ACTOR reconciliation. | Odysseus isolated-harness owner | No controller-owned credential/key, ambient daemon authority, cross-generation adoption, or duplicate uncertain effect. | Wrong-origin/store/key, pre-intent credential open, death/lost-response/restart/replay, networked session-state, secret cleanup, actor/process/store-handle absence tests. |
| A-01 | Actionable: Athena's generated 304-line contract mirrors 91 principles. | Delete the mirror and route only task-relevant context. | Athena | Preserve public skill identity and safety state machines. | Tracked/loader/package/reference search and contract tests. |
| A-02 | Actionable: Athena's plugin-only `defaultPrompt` is broad. | Remove it; keep provider-neutral authority in repository/prompt layers. | Athena | No replacement global/provider prompt. | Plugin manifest validation and prompt assembly tests. |
| A-03 | Actionable: all 17 root `SKILL.md` files are long procedural entrypoints. | Preserve the 17 IDs and make each root a discriminating router; move detailed/rare procedures to referenced resources or scripts. | Athena | Strict review, worktree, and evidence state machines remain. | Per-skill trigger matrix, reference reachability, executable workflow tests. |
| A-04 | Actionable/approval blocked: Athena validators enforce headings, phrases, catalogs, and other editorial form. | Retain metadata, dependency, package, reference, and executable-behavior checks; remove editorial assertions after any required workflow approval. | Athena | `.github/workflows/**` needs human approval. | Negative structural fixtures, package tests, exact-head CI. |
| M-01 | Actionable: Mnemosyne root/future-authoring guidance requires fixed models, five sections, size limits, and duplicated principles. | Slim only root and future-authoring guidance; remove those future requirements. | Mnemosyne | No corpus-body rewrite and no instruction-size gate. | Guidance diff plus metadata/provenance/duplicate/link tests. |
| M-02 | Frozen: current corpus has 786 entries, 183 notes, and 318 histories at tree `1e46f9fac1e46d0e0b16492a971e219d5d5de4fd`. | Grandfather and preserve every body/history byte-for-byte. | Mnemosyne | Locked corpus boundary supersedes the plan's older 783/181 counts. | Tree hash and exact file/count comparison. |
| M-03 | Frozen consumer conflict: `skills/atlas-go-dashboard-milestone-delivery.md` still directs Atlas review waves and auto-merge. | Preserve the entry; add non-entry future guidance deprecation/override or obtain separate authority for any later exception. | Mnemosyne with Argus/Odysseus migration owners | No silent edit/delete of knowledge content. | Exact entry hash plus organization-wide Atlas consumer search. |
| M-04 | Approval blocked: protected Mnemosyne workflows pin `agent-contract-v1.0.0`. | Change only after human workflow approval and the canonical release exists. | Mnemosyne | Preserve corpus and workflow security/structural gates. | Workflow diff review, resolved release, exact-head CI. |
| H-01 | Actionable: Hephaestus has a 934-line root contract and stale catalogs/settings. | Slim to repository scope, boundaries, autonomy, completion, and contextual routes; apply D-04. | Hephaestus | Preserve library/automation ownership from Accepted ADR-016 and the rebound head's Fleet build-artifact and host-capability/recovery behavior. | Contract/settings tests, focused artifact-server and recovery tests, and exact-head CI. |
| H-02 | Actionable: live prompt families recursively inject writing standards, duplicate terse directives, and fixed validation delegation. | Recompose each family around trusted operation, authority, completion, output, and ordered fenced payloads. | Hephaestus | Preserve template names/overrides, parsers, schemas, scopes, state machines, bounds, and defaults. | Internal-consumer inventory, hostile-payload/fencing tests, parser/schema/state tests. |
| H-03 | Missing behavior: no invocation-local exact-literal Astra supplement exists. | Add once for Codex plus parsed base model `gpt-6-astra`, including resumed turns; exclude empty/default, `astra`, other models, process globals, and untrusted fences. | Hephaestus | No provider/model default change. | New/resumed provider-model matrix and exact-once placement tests. |
| H-04 | Release blocked: `athena_contract.py` and its manifest hash older advise/learn content and a stale marketplace PR-review path. | Update manifest/content references only after Athena's release, then publish Hephaestus before consumers. | Hephaestus after Athena | No speculative release pin. | Manifest hashes, package resolution, release evidence. |
| H-05 | Partially corrected: the rebound head removed branch-format prose enforcement from the documentation policy and added behavioral configuration-policy tests, but terse/editorial sentinel tests still assert prompt prose. | Replace the remaining prompt-prose assertions with semantic authority, fencing, output, completion, and supplement-selection tests. | Hephaestus | Retain exact branch-identity, parser/schema/security, and resource-bound gates. | Negative semantic fixtures and exact-head CI. |
| AG-01 | Actionable: optional authored `model` is dropped across Agamemnon paths. | Round-trip it through store, OpenAPI, Python client, reconciler, rollback, and export. | Agamemnon | Do not change model defaults or selection. | Full path round-trip tests, absent-value compatibility. |
| AG-02 | Cross-repo compatibility: Agamemnon, Myrmidons, and Telemachy disagree on scalar/array `programArgs`. | Agamemnon first accepts legacy scalar at `myrmidons/v1`, normalizes internally, and always emits arrays; coordinate downstream canonicalization. | Agamemnon, Myrmidons, Telemachy | Preserve field name, argument order/value, `hi/v1`, and defaults. | Scalar/array ingress fixtures and store/export/reconcile round trips. |
| AG-03 | Documentation drift: runtime PUT and PATCH both merge, while client/agent docs promise PUT replacement; Telemachy/Odysseus rely on merge. | Correct documentation and client expectations; do not alter runtime semantics. | Agamemnon | Merge behavior is compatibility-locked. | PUT/PATCH partial-update regression tests and downstream fixtures. |
| AG-04 | Verified dead: two tracked issue-specific prompt artifacts have no retained authority. | Delete after repeated hidden/loader/test/consumer search. | Agamemnon | Preserve closed issue bodies and task text. | Exact-SHA consumer transcript and package tests. |
| AG-05 | Verified self-consumed: the local scaffolder points at its own broken template path; Myrmidons already owns canonical templates. | Remove scaffolder and docs; route template creation to Myrmidons. | Agamemnon | Cross-repo sequencing; no Myrmidons template move. | Self/external consumer search and documentation test. |
| AG-06 | Actionable: root/settings and language, peer-communication, store, and HTTP semantics are stale. | Slim/correct guidance and apply D-04. | Agamemnon | Preserve APIs, wire contracts, defaults, and security gates. | Source/OpenAPI/store fact review and exact-head CI. |
| AG-07 | Trust-boundary requirement: task descriptions currently transit the control plane. | Transport bytes unchanged and fence them as untrusted only at prompt assembly. | Agamemnon and Hephaestus | No task-body editorial rewrite in transport. | Byte round-trip plus injection/fencing tests. |
| T-01 | Actionable: Telemachy root/settings and NATS/persistence docs drift; hook-bypass guidance remains. | Slim/correct guidance, remove bypass advice, and apply D-04. | Telemachy | Preserve defaults and actual persistence behavior. | Source/doc fact checks, settings tests, exact-head CI. |
| T-02 | Verified dead: five follow-up artifacts and an obsolete skill README have no consumers. | Delete with same-PR consumer/exclusion cleanup. | Telemachy | Protected workflows need approval if touched. | Hidden/tracked/loader/test/organization search. |
| T-03 | Duplication: M0–M6 prose lives in Odysseus manifests/payloads and Telemachy lacks the complete canonical set. | Make Telemachy workflow manifests the future description source and render future milestone issues from it. | Telemachy and Odysseus | Do not rewrite closed issue bodies. | Manifest/schema/generator tests and duplicate-text search. |
| T-04 | Runtime mismatch: Docker uses removed `/v1/agents/docker`; local and Docker omit/differ on explicit program/model fields. | Use Agamemnon's canonical endpoint in both paths and forward explicit fields consistently. | Telemachy after Agamemnon | No endpoint/default compatibility guess. | Local/Docker parity and canonical-endpoint fixtures. |
| MY-01 | Actionable: Myrmidons has a 455-line architecture/catalog/manual root and legacy settings. | Slim the contract and apply D-04. | Myrmidons | Preserve schemas, desired-state authority, hooks, denies, and defaults. | Contract/settings validation and exact-head CI. |
| MY-02 | Compatibility drift: both schemas require scalar `programArgs`, and 55 manifests/templates/fleets author scalars. | After AG-02, accept legacy scalar, normalize, canonicalize authored values to `string[]`, and always emit arrays. | Myrmidons with Agamemnon/Telemachy | Preserve token order/value and existing model selections. | Schema/checker/docs/manifests tests and cross-repo fixtures. |
| MY-03 | Live-state blocked: exactly 43 active issue-specific agents remain—issue 8: 10, issue 22: 3, issue 69: 30—and three matching fleets are consumed by dynamic loaders. | Query the live reconciler and obtain operator approval; if inactive, hibernate/apply/verify shutdown in one PR, then delete in a second PR. | Myrmidons operator | No live query occurred during inventory; Git state is not shutdown evidence. | Reconciler readback, operator approval, shutdown receipt, post-delete loader/consumer search. |
| MY-04 | Authoring drift: issue/template guidance treats one-off work as durable desired-state agents. | Route future one-offs to Telemachy tasks and distinguish them from durable manifests. | Myrmidons and Telemachy | Existing desired state stays untouched until MY-03. | Template/docs review and task/manifest behavior tests. |
| MY-05 | Editorial gates: `lint-agents-md.sh` and `detect-doc-drift.sh` enforce headings/phrases/model wording. | Remove editorial assertions; retain metadata, schema, reference, parser, security, and executable-behavior checks. | Myrmidons | Workflow edits require approval. | Negative structural fixtures and exact-head CI. |
| MY-06 | Stale PR template: references nonexistent `scripts/plan.sh`/`validate.sh` and demands blanket target-host testing. | Use outcome, scope, allowed effects, relevant checks, observable completion, and stopping conditions. | Myrmidons | Do not invent unavailable local evidence. | Link/command existence and template lint. |
| MY-07 | Already aligned: Myrmidons owns four canonical templates and its package includes agents/fleets/schemas/pools. | Retain ownership; make no move. | Myrmidons | Agamemnon removes its duplicate under AG-05. | Package inventory and cross-repo consumer search. |
| MY-08 | Frozen default: issue agents use `model: null`; mesh roles and Julia/VegAI arguments carry intentional values. | Preserve values while normalizing representation. | Myrmidons | No model/lane/provider/token change. | Semantic manifest comparison. |
| AF-01 | Actionable: AchaeanFleet has a 252-line prerequisite/catalog contract and legacy settings. | Slim to boundaries/routes and apply D-04. | AchaeanFleet | Preserve runtime assignments, hooks, denies, and build/security rules. | Contract/settings tests and exact-head CI. |
| AF-02 | Topology drift: docs claim one `homeric-mesh` network; primary Compose uses `agamemnon-frontend` and `agent-backend`, and Eris/Pallas are omitted. | Make Compose/HCL/pod manifests authoritative and correct focused topology docs while preserving the separate mesh-worker topology. | AchaeanFleet | Runtime topology change requires separate behavior evidence. | Compose/HCL/pod parse and source/doc comparison. |
| AF-03 | Workspace drift: initialization/validation disagree with mounts, casing, defaults, and documented per-agent project variables have no consumer. | Align commands/validation with actual mounts and remove unconsumed claims. | AchaeanFleet | Do not broaden host mounts. | Entrypoint/Compose/justfile/env consumer tests. |
| AF-04 | Count drift: vessel/service/build totals differ across docs, Dagger, scripts, workflows, Compose, and Nomad. | Route counts to their versioned runtime sources rather than a global static catalog. | AchaeanFleet | Protected workflow matrices require approval. | 11 vessel Dockerfiles, nine production images, protected matrix, Compose and Nomad inventory comparison. |
| AF-05 | Approval blocked: `AGENT_PROGRAM` and `AGENT_HEADLESS_FLAG` have no runtime consumer; nine Dockerfile declarations remain and only protected CI checks `AGENT_PROGRAM`. | After workflow approval, move binary validation to explicit versioned data and remove both variables/docs. | AchaeanFleet | No protected workflow edit without approval. | Hidden/org consumer search and `.github/workflows/ci.yml` binary-check trace. |
| AF-06 | False-green: local CI wrapper swallows lint, Markdown, validation, and test failures and references wrong or absent task names. | Repair oracles/task names while retaining all real gates; update protected wiring only after approval. | AchaeanFleet | No gate weakening. | Deliberate failing fixtures plus Required Checks. |
| AF-07 | Current CI failure: run 34839980306 fails because three Trivy exceptions expired 2026-08-27. | Perform security review and repair/remove findings; never extend dates blindly. | AchaeanFleet security owner | Security exception change requires human review. | Vulnerability disposition and green exact-head run. |
| AF-08 | Unsafe guidance: pre-push material recommends `--no-verify` and force-with-lease recovery. | Remove bypass/force guidance; document fix-forward or new-branch recovery. | AchaeanFleet | Preserve signing/protected-branch controls. | Targeted text search and hook tests. |
| AF-09 | Documentation drift: README says every container uses an Agamemnon sidecar and `AGAMEMNON_URL` controls it. | Scope sidecar claims only to manifests that mount one; route details to runtime sources. | AchaeanFleet | Do not add mounts or sidecars. | README-to-manifest comparison. |
| AF-10 | Frozen default: Aider and mesh-worker model assignments are intentional. | Preserve them exactly. | AchaeanFleet | No model/provider change. | Compose/HCL semantic diff. |
| N-01 | Actionable: Nestor has a 147-line root contract and legacy settings. | Slim and apply D-04. | Nestor | Preserve actual security/build boundaries. | Contract/settings checks. |
| N-02 | Factual drift: root docs claim Telemachy/Keystone/BlazingMQ/`hi.pipeline` relationships unsupported by code. | Route architecture/API/NATS material to focused source-backed docs and correct claims. | Nestor | Do not invent current deployment. | Source/consumer search and docs link tests. |
| N-03 | Build drift: docs claim nats.c 3.12 and Conan; CMake uses direct FetchContent nats.c 3.9.1. | Correct language/tooling guidance. | Nestor | No dependency change in a docs-only repair. | CMake/CONTRIBUTING comparison. |
| N-04 | Interface drift: README claims `0.0.0.0:8080`; code binds `127.0.0.1:8081`. | Make README/OpenAPI/code-aligned interface documentation authoritative. | Nestor | No bind/default change. | Interface test and source/doc comparison. |
| N-05 | Orphan/unsafe authoring: skill references are missing; PR template routes public API docs into AGENTS; hook/force advice remains. | Remove orphan/bypass advice and route API docs outside AGENTS. | Nestor | Preserve real checks and protected branches. | Reference/command search and template lint. |
| N-06 | External/approval blocked: Required Checks are green, but Docker Publish run 35255742133 fails GHCR `permission_denied: read_package`. | Fix only with package/workflow authority; do not claim governance docs resolve it. | Nestor owner | Protected workflow and external package permissions. | Fresh Docker Publish run and package-permission audit. |
| AR-01 | Actionable: Argus has a 517-line root contract and legacy settings. | Slim and apply D-04. | Argus | Preserve runtime/security boundaries. | Contract/settings checks. |
| AR-02 | Factual drift: read-only/no-application claims conflict with ACKs, dispatch, and application code, including the rebound head's opt-in Fleet collector with three concurrent bounded reads; scope/concurrency/protected-path claims also drift. | Correct against source and route details to focused docs. | Argus | No runtime behavior change from docs repair; preserve the disabled-by-default Fleet setting and observation semantics. | Source/doc fact tests plus focused Fleet exporter tests. |
| AR-03 | Stale catalogs/ritual: static model/skill lists, mandatory reads, and a nonexistent cardinality-budget reference remain. | Remove catalogs/ritual and dangling reference; retain contextual routes. | Argus | No instruction-size gate. | Hidden reference search and docs lint. |
| AR-04 | Editorial tests: dashboard catalog checks enforce exact prose rather than behavior. | Replace with structural/behavior validation. | Argus | Protected workflow edits require approval. | Negative behavior fixture and exact-head CI. |
| AR-05 | Cross-repo blocked: Atlas scripts/charter appear orphaned locally but remain consumed by Odysseus justfile, six PR templates, docs, and immutable Mnemosyne guidance. | Migrate Odysseus live consumers first; preserve Mnemosyne entry under M-03; then delete Argus assets and links atomically. | Argus, Odysseus, Mnemosyne | No deletion while any executable/authoring consumer remains. | Local/org `rg` and `gh search code`, replacement behavior, final deletion search. |
| HE-01 | Actionable: Hermes has a 487-line root contract and legacy settings. | Slim and apply D-04. | Hermes | Retain async/subject/wire/config/lock/HMAC invariants. | Contract/settings checks plus invariant tests. |
| HE-02 | Factual drift: guidance claims synchronous Agamemnon and stateless behavior and includes speculative recipes. | Remove contradictions; make README/OpenAPI the interface authority. | Hermes | Preserve implemented async/state behavior. | Source/OpenAPI/README comparison and integration tests. |
| HE-03 | False-green: `scripts/run_ci_local.sh` swallows missing Markdown tooling/lint failures with a logical-OR `true`; no markdownlint dependency exists, and the local suppression checker is weak. | Add a real dependency/path and fail on missing/lint/suppression errors. | Hermes | Protected workflow wiring needs approval; retain Markdown and suppression gates. | Deliberate missing-tool/lint/suppression failures and exact-head CI. |
| HE-04 | Unsafe routing/guidance: comments route public API material to AGENTS and bypass advice remains. | Route interface docs to README/OpenAPI and remove bypass advice. | Hermes | Preserve review/security checks. | Targeted search and docs lint. |
| HE-05 | Frozen governance: two different Accepted ADR-004 bodies exist. | Preserve both bodies; make only index/compatibility decisions outside them. | Hermes | Accepted ADRs append-only. | Exact blob hashes and index validation. |
| C-01 | Actionable: Charybdis has a 269-line contract and legacy settings. | Slim and apply D-04 while retaining isolation, cleanup, authorization, and sanitizer boundaries. | Charybdis | No wider chaos authority or combined sanitizers. | Contract/settings checks and exact-head CI. |
| C-02 | Missing enforcement: runtime lacks an explicit chaos-authorization check. | Require explicit authorization before every fault and prove denial otherwise. | Charybdis | Agamemnon remains the sole fault-injection API. | Authorized/unauthorized behavior tests and API trace. |
| C-03 | Cleanup defect: cleanup ignores DELETE failure and does not prove terminal absence. | Propagate deletion failure and require independent absence readback. | Charybdis | Guaranteed cleanup remains a hard invariant. | Failure injection, retry/reconcile, and absence tests. |
| C-04 | ROADMAP drift: items at lines 57/61 reference issues that are closed. | Mark only verified completed work accordingly. | Charybdis | Closed issue bodies remain immutable. | Issue-state read and roadmap link check. |
| C-05 | Validation drift: local sanitizer path can skip while CI is fail-closed. | Make local results truthful and keep ASAN and TSAN separate. | Charybdis | No skipped run reported as success; workflow edits need approval. | Missing-tool and sanitizer-failure fixtures plus separate CI jobs. |
| C-06 | Cross-repo fact drift: Charybdis's live Agamemnon `/v1/chaos/*` contract is exercised by source/client/OpenAPI/tests and Odysseus E2E, while Agamemnon docs say injection is not wired. | Correct Agamemnon docs; preserve endpoint and consumers. | Agamemnon documentation owner | No endpoint/default change. | Exact-SHA consumer inventory and contract tests. |
| K-01 | Actionable: Keystone has a 405-line contract, 143-line stale `.clinerules`, and legacy settings. | Slim, replace `.clinerules` with a minimal AGENTS compatibility pointer, and apply D-04. | Keystone | Preserve current runtime/security rules. | Pointer, settings, docs tests. |
| K-02 | Verified obsolete: 34-agent hierarchy and agent-only scaffolding have no live external runtime consumer. | First move unique current C++ rules to focused docs, then delete hierarchy/scaffolding and references atomically. | Keystone | No rule loss; protected checks need approval. | Rule matrix, hidden/loader/org consumer search, build/security tests. |
| K-03 | Verified dead: `setup-notes.sh` has no external consumer. | Delete with same-PR reference/package cleanup. | Keystone | No notes-path replacement needed unless a consumer appears at rebind. | Hidden/tracked/org consumer search. |
| K-04 | Editorial gate: docs-tree validation enforces AGENTS prose. | Retarget it to pointers, references, and focused-doc structure. | Keystone | Workflow edit requires approval. | Negative structural fixture and exact-head CI. |
| K-05 | Unsafe guidance: bypass advice remains. | Remove it and retain fix-forward guidance. | Keystone | No hook/security weakening. | Targeted search and hook tests. |
| P-01 | Actionable: Proteus has a 310-line contract and legacy settings. | Slim and apply D-04. | Proteus | Preserve hard security denies and repository dispatch. | Contract/settings checks. |
| P-02 | Verified dead: `.claude-followup-103.json`, `.claude-followup-6.md`, `.claude-followup-82.json`, and `.followup-94.json` have no consumers. | Delete all four with reference/exclusion cleanup. | Proteus | Recheck hidden and organization consumers at PR head. | `git ls-files`, hidden `rg`, loader/package/test search, `gh search code`. |
| P-03 | Documentation drift: issue status, pytest/integration coverage, gitleaks, and dispatch claims are inaccurate. | Correct each against source and hosted checks. | Proteus | Preserve `repository_dispatch` and hard security denies. | Source/doc comparison and behavior/security tests. |
| P-04 | CHANGELOG contradiction: guidance conflicts on generated/manual ownership. | Make the actual release process authoritative. | Proteus | Do not rewrite historical releases. | Generator/release-script and docs comparison. |
| P-05 | False-green: dependency/suppression checks can mask failure. | Fail closed and retain security coverage. | Proteus | Protected workflow edits require approval. | Deliberate dependency/suppression failure fixtures and exact-head CI. |
| P-06 | Overvalidation: every edit is told to run full `just validate`. | Select affected path-scoped checks and reserve full suites for runtime/API/schema/security changes. | Proteus | No skipped relevant check. | Affected-path routing matrix and CI equivalence tests. |
| O-01 | Actionable: Odyssey has a 1,119-line contract, huge live prompt/command/resource inventory, and legacy settings. | Slim root guidance, modernize live surfaces contextually, and apply D-04. | Odyssey | Preserve repository-specific safety-net behavior. | Full tracked inventory, loader/consumer tests, exact-head CI. |
| O-02 | Role overlap: 29 roles exist; the locked live set is nine named roles. | Retain/rewrite exactly nine and remove the other 20 plus template only after every consumer migrates. | Odyssey | Preserve frozen Scylla/Mnemosyne external copies. | Exact 29→9 manifest/consumer inventory and deletion search. |
| O-03 | Atomic dependency: chief-architect is widely consumed by `plan_issues.py`. | Refactor both in one change. | Odyssey | Do not strand planning consumers. | Planning-script integration fixtures. |
| O-04 | Prompt/tool authority drift: live prompts are unfenced, tools are broad, and instructions allow `git add -A`, push, and unconditional auto-merge. | Fence untrusted inputs, scope tools/effects, and require exact review/CI authority before merge. | Odyssey | No force/bypass/automatic remote effect from candidate text. | Hostile prompt, tool-scope, Git-effect, review/CI tests. |
| O-05 | Host bypass: host-side permission bypass is enabled. | Remove it and fail closed under an explicit read-only tool policy. | Odyssey | No implicit host authority. | Negative permission/tool-policy tests. |
| O-06 | Factual/catalog drift: Mojo versions, MCP recommendations, model guidance, and marketplace entries are stale or nonexistent. | Remove static guidance/entries and route to current focused sources. | Odyssey | Preserve actual supported runtimes/defaults. | Package/marketplace/reference searches and build tests. |
| O-07 | Verified obsolete: skill-migration wiring, scripts/tests/docs, and protected workflow paths are self-contained. | Remove only after consumer migration; obtain workflow approval. | Odyssey | Protected workflow and frozen external references. | Internal/external consumer search and exact-head CI. |
| S-01 | Actionable: Scylla has a 623-line contract, eight operational agents, dense judge/tier prompts, and legacy settings. | Slim/routerize while preserving JSON-only scoring, pre/post meaning, no-code-execution, remote-write, and cleanup boundaries; apply D-04. | Scylla | No scoring or default change. | Prompt/parser/scoring/cleanup tests and exact-head CI. |
| S-02 | Adapter defect: `openai_codex.py` invokes stale `codex --model ... --quiet` without `exec`; tests lock the bad form. | Use `codex exec` and update behavior tests. | Scylla | No new model default. | Exact argv fixtures and real CLI-compatible integration test. |
| S-03 | Provider defect: `subtest_executor.py` silently defaults to Claude despite configured provider/adapter. | Thread explicit provider selection end-to-end and fail on missing/unknown selection. | Scylla | No provider default. | Provider matrix and unknown/missing negative tests. |
| S-04 | Historical deletion: commit `acf03e5e` removed benchmark inputs; parent `f2f67270e5ee4d79938e33470c4bdbd1c48fda1d` contains skills tree `5e1f7a46de35b948b805940c95c0f99c509efa6e` with 119 files and 82 `SKILL.md` files. | Restore exact Git objects into a versioned legacy cohort with provenance/hashes; update composer/generator/counts only. | Scylla | No Astra cohort and no content reinterpretation. | Object/tree hashes, 119/82 counts, composer/generator tests. |
| S-05 | Deletion candidate: 40 unregistered legacy skills—39 plugin plus `docs-status-fix`—have internal validator/pre-commit references but no external live consumer. | Move unique current Scylla rules to focused docs and delete skills plus internal references in the same PR. | Scylla | Preserve historical cohort and frozen evaluation inputs. | Hidden/local/org consumer search, package/validator tests, deletion search. |
| S-06 | Editorial gates enforce prompt catalogs/prose. | Remove editorial assertions; retain schema, JSON, scoring, reference, package, permission, and behavior checks. | Scylla | Workflow edit requires approval. | Negative behavior fixtures and exact-head CI. |
| S-07 | Current protected-check weakness: npm audit is false-green. | Repair after workflow approval; do not suppress or relabel failure. | Scylla | Security gate remains. | Vulnerable fixture and green exact-head protected run. |
| S-08 | Frozen evidence: blocks tree `39c0471e5d3b6df17a47969c6ff2715a55762ddb`, agents tree `b026eb44aca7a05c71ae1a2aea18c367abf1091d`, subtests tree `6ac593630ba98ee87ad851b1323361b8884c47a8`, tiers blob `5c54a3edce5fea495c72991e33f43ee5da4288d0`, fixture-tiers tree `5fcfdb181f0909dc9cc4ba6f5b12431ffdf4e1a1`, and raw tree `729664a6460de923b1640a27e653c62d6075d0fc`. | Preserve exact objects. | Scylla | Never edit blocks, agents, subtests, tiers, or `docs/arxiv/dryrun/raw/**`. | Git object/hash comparison before and after every Scylla PR. |
| OD-01 | Partially corrected: this tranche now states the live skip-permissions workspace and credential/config exposure truthfully and identifies Proposed ADR-025 as unimplemented. After ADR-022, Odysseus root guidance still needs slimming and correction for counts, architecture/status, knowledge fallback, ai-maestro, remaining mount/runbook/onboarding, and direct-script claims. | Preserve the source-backed current-risk statement; slim and route the remaining context in the later Wave-1 PR. | Odysseus / this governance change and later Wave-1 PR | Accepted ADRs, configs, defaults, harness behavior, and closed issues remain unchanged. | Exact harness/source anchors and targeted policy search now; remaining source-backed docs tests and exact-head CI later. |
| OD-02 | Duplication: future milestone issue prose is maintained outside Telemachy. | After T-03, generate future issues from canonical manifests. | Odysseus and Telemachy | Do not rewrite closed issues. | Generator/golden-structure tests without prompt-text snapshots. |
| OD-03 | Template drift: issue/PR templates demand blanket suites and submodule pins. | Use outcome, scope, allowed effects, relevant checks, completion, and stop conditions. | Odysseus | Gitlink changes retain integration approval. | Template lint and command/reference existence. |
| OD-04 | Installer drift: tooling duplicates Hephaestus skills and uses `$HOME/.agent-brain/Mnemosyne` instead of `.agent_brain/knowledge`. | Install only Athena plugin skills and use the canonical knowledge path. | Odysseus after Athena release | No user-cache or unrelated home mutation. | Installer sandbox test and installed-tree assertion. |
| OD-05 | False-green: hierarchy sync can report success when submodules are absent and lacks the documented `just` entrypoint. | Report unavailable/failure truthfully and add the documented entrypoint. | Odysseus | Missing dependencies are not success. | Empty-submodule negative fixture and `just` invocation test. |
| OD-06 | Interim harness risk: live legacy paths trust issue text, fall back malformed routing to all repos, accept loose verdicts, suppress invocation failures, and can complete without terminal evidence. | Fence payloads, reject malformed routes, require exact verdict schemas, propagate failures, enforce scopes, and gate completion on verified evidence until G-31. | Odysseus / later Wave-1 PR | Preserve current provider/model/lane/role defaults. | Hostile payload/routing/verdict/failure/protected-scope/terminal-evidence tests. |

## Evidence and limits

The checks below record earlier validation runs. Subsequent review repairs
changed the candidate; they are not evidence that the publication commit
passes. Fresh required CI and Athena review remain pending on that commit.

- The clean-base documentation command used by current CI is
  `markdownlint AGENTS.md docs/architecture.md docs/adr/*.md`. The host did not
  have a global `markdownlint` binary, so the baseline used an ephemeral
  `markdownlint-cli` invocation matching CI. It passed before edits.
- Post-repair Markdownlint passed for every changed Markdown file and every
  ADR. The workflow-equivalent Python Markdown parser parsed all 55 files under
  `docs/`.
- The post-repair structural check found 25 unique ADR numbers with matching
  filenames and headings, body/index statuses, complete rows in both indexes,
  and reachable local links outside fenced examples and the ADR template's
  explicit placeholder.
- Ten Accepted or Superseded ADR bodies are byte-unchanged. Fleet ADR-021 is
  byte-unchanged at Git blob
  `84cacc68b7dd6bb33748e27bb9a129923b37a5db`.
- `CLAUDE.md` remains the exact 122-byte, three-line compatibility pointer.
- `git diff --check` passed, and scoped diffs for `.github/workflows/**`,
  `configs/nats/**`, `configs/nomad/**`, `.gitmodules`, and all gitlinks are
  empty.
- This tranche includes documentation, schema descriptions, task manifests,
  and executable validation changes. Tailscale is unavailable on this host;
  hosted CI must validate platform-dependent behavior. ADR-025 requires real
  Linux/rootless-Podman CI for a
  later implementation; portable or macOS tests cannot substitute for it.
- No result in this ledger claims that ADR-022, ADR-023, ADR-024, or ADR-025 is
  Accepted, implemented, deployed, or binding architecture.
- Independent exact-byte review findings are repair inputs, not approval. This
  ledger records the current repaired bytes and does not claim review GO; the
  final immutable PR head still requires fresh Athena review and required CI.
- A truthful failed or unavailable check remains a blocker or limitation; it
  is never rewritten as completion evidence.

### Earlier key-artifact digests

These SHA-256 values identify earlier validation inputs, not the current
candidate after subsequent repairs. The publication commit binds the complete
candidate, including this ledger. Do not use these older hashes as review
approval for that commit.

| Path | SHA-256 |
|---|---|
| `docs/adr/022-layered-provider-neutral-agent-instructions.md` | `4a37878a8c48e9bc867885e989ab77343d3c77ac105208a7d79918c3d0d54265` |
| `docs/adr/023-defer-multi-host-nomad-scheduling.md` | `4639fd91c389e7fb27dcffba7161af54eade9452f4d87c235e629ad92b1d511a` |
| `docs/adr/024-one-homeric-nats-application-account.md` | `438c2e9c2986d1416dbd7fee89be1285bfce2fd48878cdb029188a59a6367b75` |
| `docs/adr/025-isolate-legacy-credential-broker.md` | `dc66769e8e07963e04c3c794eaddd5710a521f717e9af3647f238d307f2a9c2d` |
| `docs/architecture.md` | `9dab4eecd70f6d2b43c4a0818cbccd8b37a63dd1c0d733c87ba64735585b8719` |

## Protected files and states

This tranche must leave all of the following byte-unchanged:

- every ADR body whose recorded status is Accepted or Superseded;
- `.github/workflows/**`;
- `configs/nats/**` and `configs/nomad/**`;
- `.gitmodules` and every gitlink; and
- every component repository, live reconciler, credential, host, and deployed
  service.

Formal acceptance of a Proposed ADR requires an explicit reviewed status-change
pull request. Merge of this governance tranche alone is not acceptance and
does not release any implementation dependency.
